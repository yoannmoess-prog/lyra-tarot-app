# Clé API pour le service LLM (ex: OpenAI).
# Remplacez "YOUR_API_KEY_HERE" par votre véritable clé.
LLM_API_KEY="YOUR_API_KEY_HERE"

# (Optionnel) URL de base pour l'API LLM, si vous utilisez un proxy ou un service autre qu'OpenAI.
# LLM_BASE_URL="https://api.openai.com"

# (Optionnel) Modèle LLM à utiliser.
# LLM_MODEL="gpt-4o-mini"

# (Optionnel) Activer le RAG (Retrieval-Augmented Generation)
# RAG_ENABLE="0"